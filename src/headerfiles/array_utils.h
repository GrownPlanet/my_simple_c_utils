#ifndef ARRAY_UTILS_H
#define ARRAY_UTILS_H

#include <stddef.h>
#include <stdio.h>

// print an intiger array
void ARRAYU_print_int(int* arr, size_t len);

// print an string array
void ARRAYU_print_string(char** arr, size_t len);

#endif
